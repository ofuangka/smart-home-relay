{
	"name": "z-way-relay",
	"version": "1.0.0",
	"description": "Relays authenticated requests to the local z-way server",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.17.2",
		"express": "^4.15.2"
	}
}
